# Site-wide HTTP Basic auth gate (src/proxy.ts).
# Copy to .env.local and change these before deploying anywhere public —
# the fallbacks compiled into src/proxy.ts are committed to git and are
# therefore not secret.
SITE_AUTH_USER=fridayradar
SITE_AUTH_PASSWORD=friday-night-lights
